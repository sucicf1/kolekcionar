{% extends "::layout.html.twig" %}

{% block container %}
    {% for item in items %}
        {{item.name}} <br />
        {{item.price}} <br />
        {{item.productionDate|date('d.m.y')}} <br />
        {{item.qualityPercent}} <br />
    {% endfor %}
{% endblock %}